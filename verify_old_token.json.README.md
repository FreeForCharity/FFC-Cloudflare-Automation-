# verify_old_token.json

This file contains a sample Cloudflare API response for an invalid/expired API token. It is used for testing and verification purposes to demonstrate the error response structure returned by the Cloudflare API when authentication fails.
